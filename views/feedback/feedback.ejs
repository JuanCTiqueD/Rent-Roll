<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/feedback.css">
    <title>Feedback - Rent&Roll</title>
</head>
<body>
    <div class="container-fluid p-0">
        <div class="row g-0 full-height-container">
            <div class="col-md-5 image-container">
                <img src="/images/car3.jpg" alt="Feedback Image">
            </div>
            <div class="col-md-7 content-container">
                <div class="text-center mb-4">
                    <h1 class="logo-text">Rent & Roll</h1>
                    <h2>Ingresa tus comentarios y sugerencias</h2>
                    
                    <form action="/feedback" method="post">
                        <% if (user) { %>
                            <input type="hidden" name="id_usuario" value="<%= user.id_usuario %>">
                        <% } %>
                        <% if (reserva) { %>
                            <input type="hidden" name="id_reserva" value="<%= reserva.id_reserva %>">
                        <% } %>
                        
                        <div class="mb-4">
                            <label for="comentario" class="form-label">Mensaje</label>
                            <textarea class="form-control message-box" id="comentario" name="comentario" rows="5" required></textarea>
                        </div>
                        
                        <div class="rating-section mb-4">
                            <label for="calificacion" class="form-label">Del 1 al 10, ¿cómo nos califica?</label>
                            <select id="calificacion" name="calificacion" class="form-select" required>
                                <option value="" disabled selected>Seleccione una calificación</option>
                                <% for (let i = 1; i <= 10; i++) { %>
                                    <option value="<%= i %>"><%= i %></option>
                                <% } %>
                            </select>
                        </div>
                        
                        <button type="submit" class="btn btn-primary btn-lg">Enviar</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
